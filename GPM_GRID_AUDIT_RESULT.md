# GPM_GRID_AUDIT_RESULT — GPM 栅格网格预检

> 生成日期：2026-08-14
> 扫描脚本：`scripts/audit_gpm_grid.py`

## 1. 检查结果

| 项 | 值 |
|----|----|
| 扫描文件数 | 400（共 33420 个 TIF，抽样 400） |
| 唯一 CRS | **1**（全部 EPSG:4326） |
| 唯一像素分辨率 | **1**（0.1° × 0.1°） |
| 唯一栅格尺寸 | **1**（201 × 201） |
| 唯一 nodata | **1**（-9999.0） |
| 唯一 transform | **400**（经度 origin 逐文件漂移） |

## 2. 结论：FAIL（预期内的"多 transform"）

预检脚本按"transform 唯一"判定为 FAIL，但**根因是已知的瓦片结构，而非损坏或异构网格**：

- 每个 TIF 是**台风中心裁剪的独立瓦片**（文件名含 `center_<lon>E_<lat>N_bbox_...`）。
- 所有瓦片共享相同的：CRS（EPSG:4326）、像素分辨率（0.1°）、尺寸（201×201）、nodata（-9999）、南上存储方向。
- 唯一差异是 `x_origin`（经度），因为每个瓦片以不同台风经度为中心。

## 3. 处理方式

在 `scripts/build_paper_dataset.py` 中，**不再假设单一全局参考 transform**。改为：

1. 以锚点经纬度定义**规范北向锚点网格**（0.1°、128×128）。
2. 每个 GPM/DEM 栅格用 `rasterio.warp.reproject` **各自重采样到该锚点网格**（使用各自 transform/CRS）。

这正确处理了逐文件瓦片偏移，并统一了南北方向。

## 4. 无损坏文件

未发现损坏/无法读取的 TIF 文件。
