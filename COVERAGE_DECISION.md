# COVERAGE_DECISION — GPM 时间戳匹配策略

> 生成日期：2026-08-14
> 依据：`DATA_COVERAGE_EXACT.csv` 与 `DATA_COVERAGE_T15.csv`

## 1. EXACT vs T15 对比

| 指标 | EXACT (tolerance=0) | T15 (tolerance=900s) |
|------|:---:|:---:|
| 台风事件数 | 302 | 302 |
| 网格帧总数 | 316880 | 316880 |
| 精确匹配 | 7318 | 7318 |
| 近邻匹配 (≤15min) | 0 | **0** |
| 缺失帧 | 309562 | 309562 |
| 可用 11→1 窗口 | 6929 | 6929 |

**结论：EXACT 与 T15 完全一致。** 近邻匹配（±15min）没有带来任何额外匹配——GPM 时间戳要么与 CMA 网格精确对齐，要么完全缺失。

## 2. 是否存在系统时间戳偏移？

**否。** 若存在 ±15min 系统偏移，T15 的近邻匹配数应显著大于 0，但实测为 0。因此不存在"隐藏的系统约定偏移"需要被近邻匹配掩盖。

## 3. 为何覆盖率低（2.3%）？

`TIFdata/` 目录只预处理了 **36 个台风事件**的 GPM 降水瓦片（其余 266 个 CMA 台风无 GPM 数据）。这 36 个事件共产生 6929 个可用窗口，与旧管线 6921 样本一致。覆盖率低是**数据可得性**问题，不是时间戳错配问题。

## 4. 最终推荐

```
MATCH_POLICY = EXACT
```

**理由**：GPM 时间戳与 CMA 0.5h 网格精确对齐；±15min 近邻匹配无任何收益；不存在系统偏移需修正。

## 5. 附带发现

- `typhoon_id=0000`（CMA 无名风暴，`parts[4]="0000"`）在覆盖报告中被合并成一个伪事件（约 20 万帧），但**无任何可用窗口**，不影响数据集构建。建议在正式解析中跳过无名风暴。
